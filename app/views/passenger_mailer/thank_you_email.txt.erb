<%= "Booking \##{@passenger.booking.id}" %>
<%= "Flight from #{@passenger.flight.from_airport.name} " +
    "to #{@passenger.flight.to_airport.name} " +
    "on #{@passenger.flight.start}"%>
<%= Name: @passenger.name %>
